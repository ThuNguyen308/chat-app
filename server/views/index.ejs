<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link href="/css/style.css" rel="stylesheet" type="text/css" />
    <script>
      console.log("status", "<%= status %>");
      var status = "<%= status %>";
      if (status == "verified") {
        if (confirm("Do you want to login?")) {
          window.location.href = "http://localhost:3000/";
        }
      }
    </script>
  </head>
  <body>
    <form method="post" action="" className="container-form">
      <div className="form-wrapper">
        <div className="form-inner">
          <div className="form-group">
            <h1
              style="font-size: 2rem; text-align: center; margin-bottom: 20px"
            >
              Reset Password
              <!-- <%= email%> -->
            </h1>
            <label html="password">New Password</label>
            <div className="input-group">
              <input type="password" id="password" name="password" />
              <button className="btn input-group-btn">Hide</button>
            </div>
          </div>

          <div className="form-group">
            <label html="password-confirm">Confirm Password</label>
            <div className="input-group">
              <input
                type="password"
                id="password-confirm"
                name="confirm-password"
              />
              <button className="btn input-group-btn">Hide</button>
            </div>
          </div>
          <button className="btn btn-primary" type="submit">Submit</button>
        </div>
      </div>
    </form>
  </body>
</html>
